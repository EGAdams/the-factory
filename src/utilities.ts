// 	/**
//  * Capitalize the first letter of a string
//  * @param {string} stringToUppercase - The string to capitalize 
//  * @returns {string} The capitalized string
//  */
// static capitalizeFirstLetter(stringToUppercase: string): string {
//   return stringToUppercase.charAt(0).toUpperCase() + stringToUppercase.slice(1);
// }

// /**
//  * Check if an object exists in an array 
//  * @param {any} objectToSearchFor - The object to search for
//  * @param {any[]} arrayToSearch - The array to search in 
//  * @returns {boolean} True if the object is found, false otherwise
//  */
// static isInArray(objectToSearchFor: any, arrayToSearch: any[]): boolean {
//   return arrayToSearch.indexOf(objectToSearchFor) > -1;
// }

// /**
//  * Throw an error if a condition is false
//  * @param {any} condition - The condition to check
//  * @param {string} [msg] - An optional error message
//  */ 
// static assert(condition: any, msg?: string): asserts condition {
//   if (!condition) {
//     throw new Error(msg);
//   }
// }
